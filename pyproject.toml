[build-system]
requires = ['setuptools>=40.8.0', 'wheel']
build-backend = 'setuptools.build_meta:__legacy__'

[tool.ruff]
extend-select = ["C4", "SIM", "TCH", "DJ"]
line-length = 100
show-fixes = true

[tool.black]
line-length = 100
exclude = '''
env/*
'''
